gui:
  showIcons: true
  showFileIcons: true
  nerdFontsVersion: "3"
  splitDiff: auto
  sidePanelWidth: 0.2
git:
  pagers:
    - colorArg: always
      pager: delta --paging=never --line-numbers
    - colorArg: always
      pager: delta --side-by-side --paging=never --line-numbers
